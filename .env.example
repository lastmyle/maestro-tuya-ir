# Server Configuration
# Port for the FastAPI development server (used by `make dev` and VS Code debugger)
PORT=8000

# Vercel Configuration (optional - only needed for deployment)
# VERCEL_PROJECT_ID=your_project_id_here
